MYIP=$(cat /usr/bin/ipsave)
ISP=$(cat /etc/xray/isp)
CITY=$(cat /etc/xray/city)
domain=$(cat /etc/xray/domain)
CHATID="1962241851"
KEY="6866097221:AAFdDsbTF-R7_d07ewI3z0BQHYrd7yQNhhA"
TIME="10"
URL="https://api.telegram.org/bot$KEY/sendMessage"
clear
rm -f /usr/bin/user
username=$(curl -sS $(cat /etc/xray/license_server 2>/dev/null || echo "http://tunnel.77speed.my.id")/ip | grep $MYIP | awk '{print $2}')
echo "$username" >/usr/bin/user
rm -f /usr/bin/e
valid=$(curl -sS $(cat /etc/xray/license_server 2>/dev/null || echo "http://tunnel.77speed.my.id")/ip | grep $MYIP | awk '{print $3}')
echo "$valid" > /usr/bin/e
clear
clear
d1=$(date -d "$valid" +%s)
d2=$(date -d "$today" +%s)
certifacate=$(((d1 - d2) / 86400))
if [[ $certifacate -le 1 ]]; then
CHATID="$CHATID"
KEY="$KEY"
TIME="$TIME"
URL="$URL"
TEXT="◇━━━━━━━━━━━━━━◇
<b>   ⚠️SC EXPAIRED NOTIF⚠️</b>
<b>     Detail VPS Exp</b>
◇━━━━━━━━━━━━━━◇
<b>IP VPS  :</b> ${MYIP}
<b>DOMAIN  :</b> ${domain}
<b>ISP     :</b> ${ISP}
<b>CITY    :</b> ${CITY}
<b>USER    :</b> ${username}
<b>MASA    :</b> $certifacate DAY
◇━━━━━━━━━━━━━━◇
Silahkan Perpannjang VPS Anda
"
curl -s --max-time $TIME -d "chat_id=$CHATID&disable_web_page_preview=1&text=$TEXT&parse_mode=html" $URL >/dev/null
else
echo -ne
fi